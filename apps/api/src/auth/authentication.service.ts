/**
 * Authentication Service — REMOVED
 *
 * All authentication (login, register, refresh, logout, changePassword)
 * is now handled by the .NET Backend at /identity/Auth/*.
 *
 * Angular apps call the .NET Backend directly via IdentityClient.
 * This file is intentionally left empty.
 */
