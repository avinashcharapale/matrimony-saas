import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.matrimony.saas',
  appName: 'Matrimony SaaS',
  webDir: 'dist/apps/mobile-ionic',
  bundledWebRuntime: false,
};

export default config;
